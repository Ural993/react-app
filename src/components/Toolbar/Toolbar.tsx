import React, { ReactElement } from 'react';

import './Toolbar.scss';

export const Toolbar = (): ReactElement => (
  <div className="toolbar">
    <div className="toolbar-content">Строительно-монтажные работы</div>
  </div>
);
